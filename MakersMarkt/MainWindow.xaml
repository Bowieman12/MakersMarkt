<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MakersMarkt.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MakersMarkt"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MakersMarkt">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- HEADER met Search Bar en Cart -->
        <Border Grid.Row="0" Background="#F4EBD0" Padding="12" BorderBrush="#333333" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="MakersMarkt"
                           FontSize="24"
                           FontWeight="Bold" 
                           Foreground="#333333"
                           VerticalAlignment="Center"/>

                <!-- Search Bar -->
                <AutoSuggestBox Grid.Column="1" 
                                PlaceholderText="Zoeken..." 
                                Width="300" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center"/>
                
                <!-- Account Sectie (User Info, Login, Logout) -->
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right" 
                            Margin="0,0,16,0" 
                            VerticalAlignment="Center"
                            Spacing="12"> <!-- Spacing toegevoegd voor wat ruimte tussen de elementen -->

                    <!-- User Info (Verborgen als niet ingelogd) -->
                    <StackPanel x:Name="UserInfoPanel" 
                                Orientation="Horizontal" 
                                VerticalAlignment="Center" 
                                Spacing="8"
                                Visibility="Collapsed">
                        <FontIcon Glyph="&#xE77B;" FontSize="16" Foreground="#333333" /> <!-- Persoon Icon -->
                        <TextBlock x:Name="UsernameTextBlock" 
                                   Text="Gebruikersnaam" 
                                   FontWeight="SemiBold" 
                                   Foreground="#333333" 
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Inlog Button -->
                    <Button x:Name="LoginButton"
                            Background="#E2725B" 
                            Foreground="#FFFFFF"
                            Visibility="Visible"
                            Click="LoginButton_Click">
                        <StackPanel>
                            <TextBlock Text="Inloggen" />
                        </StackPanel>
                    </Button>

                    <!-- Uitlog Button -->
                    <Button x:Name="LogoutButton"
                            Background="#E2725B" 
                            Foreground="#FFFFFF"
                            Visibility="Collapsed"
                            Click="LogoutButton_Click">
                        <StackPanel>
                            <TextBlock Text="Uitloggen" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Shopping Cart Pagina Knop -->
                <Button Grid.Column="2" 
                        Background="#E2725B" 
                        Foreground="#FFFFFF"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE7BF;" FontSize="16" />
                        <TextBlock Text="Winkelwagen" />
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- ZIJ-MENU -->
        <NavigationView x:Name="NavView" 
                        Grid.Row="1"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        ItemInvoked="NavView_ItemInvoked"
                        Background="#F4EBD0">
            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="#F4EBD0" />
                <SolidColorBrush x:Key="NavigationViewExpandedPaneBackground" Color="#F4EBD0" />
                <SolidColorBrush x:Key="NavigationViewItemForeground" Color="#333333" />
                <SolidColorBrush x:Key="NavigationViewItemForegroundPointerOver" Color="#E2725B" />
                <SolidColorBrush x:Key="NavigationViewItemForegroundSelected" Color="#E2725B" />
            </NavigationView.Resources>

            <NavigationView.MenuItems>
                <NavigationViewItem Tag="Home" Icon="Home" Content="Home" Foreground="#333333" />
                <NavigationViewItem Tag="MyProducten" Icon="Folder" Content="Mijn Producten" Foreground="#333333" />
                <NavigationViewItem Tag="AllProducts" Icon="Globe" Content="Alle Products" Foreground="#333333" />

            </NavigationView.MenuItems>

            <Border BorderBrush="#333333" BorderThickness="1,0,0,0">
                <Frame x:Name="contentFrame" />
            </Border>
        </NavigationView>
    </Grid>
</Window>